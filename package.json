{
  "name": "@set399/wawautils",
  "version": "1.0.1",
  "description": "A module designed to replace Node.js's functions like FS managment with unnecessary callbacks and promises with better functions with built-in error handlers, along with a built-in (pseudo-)random command for character and number generation",
  "license": "ISC",
  "author": "set399",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
