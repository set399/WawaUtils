# WawaUtils
An NPM package designed to replace Node.js's functions like FS managment with unnecessary callbacks and promises with better functions with built-in error handlers, along with a built-in (pseudo-)random command for character and number generation
